import { RedisLinkInfo } from '@/types';
import { invoke } from '@tauri-apps/api/tauri';
//   greetMsg.value = await invoke("greet", { name: name.value });
export function saveLink(data: RedisLinkInfo) {
  
}
